<!DOCTYPE html>
<html>
<head>
  <title><%= yield(:title) %> | BluePrintDirectory</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <header class="navbar navbar-fixed-top">
    <div class="container">
      <%= link_to image_tag("bp_logo.png", alt: "Blueprint Health logo"), root_url, class: "navbar-left" %>
      <% if logged_in? %>
      <%= link_to current_user.name, current_user, class: " navbar-right" %>
      <% else %>
      <%= link_to "Login", login_path, class: "navbar-right" %>
      <% end %>
    </div>
  </header>
  <div class="container">
    <%= yield %>
  </div>
</body>
</html>
